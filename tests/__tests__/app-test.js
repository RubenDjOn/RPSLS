/** @jsx React.DOM */

/*jest.dontMock('../../public/scripts/App.react');

var React =  require('react/addons'),
    TestUtils = React.addons.TestUtils,
    App = require('../../public/scripts/App.react'),
    appRendered = TestUtils.renderIntoDocument(<App />);


describe('_getWinner', function() {
    it('userMovement: spock, computerMovement: scissors User wins', function (done) {
        var userMovement = 'spock',
            computerMovement = 'scissors';
        
        var result = appRendered._getWinner(userMovement, computerMovement);

        expect(result).toBe('user');
    });

    it('userMovement: rock, computerMovement: paper Computer Wins', function (done) {
        var userMovement = 'rock',
            computerMovement = 'paper';

        var result = appRendered._getWinner(userMovement, computerMovement);

        expect(result).toBe('computer');
    });

    it('userMovement: spock, computerMovement: lizard Computer Wins', function (done) {
        var userMovement = 'spock',
            computerMovement = 'lizard';

        var result = appRendered._getWinner(userMovement, computerMovement);

        expect(result).toBe('computer');
    });

    describe('userMovement: scissors, computerMovement: rock Computer Wins', function() {
        var userMovement = 'scissors',
            computerMovement = 'rock';

        var result = appRendered._getWinner(userMovement, computerMovement);

        expect(result).toBe('computer');
    });
});*/